# rmp/shared-util

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rmp/shared-util` to build the library.

## Running unit tests

Run `nx test rmp/shared-util` to execute the unit tests via [Jest](https://jestjs.io).

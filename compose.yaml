services:
  frontend:
    image: apps-rmp-invoice:latest
    ports:
      - "3000:80"    
    environment:
      - API_URL=http://backend:3333/

  backend:
    image: apps-api:latest
    ports:
      - "3333:3333"
    env_file:
      - apps/api/.env
  